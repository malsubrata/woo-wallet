# Grunt
/node_modules/
skills-lock.json

# Sass
.sass-cache/
/build/

# WordPress.org SVN packaging output
/dist/

# Claude/AI
# The shared agentic architecture (CLAUDE.md, .claude/{agents,skills,rules,hooks,commands},
# docs/) IS version-controlled — it is project infrastructure, not local scratch.
# Only per-developer and agent-generated state is ignored.
.claude/agent-memory/
.claude/settings.local.json
.claude/.scratch/
.agents/
# Generated agent/tool output — not project knowledge
docs/superpowers/
docs/QA_SETTINGS_EXTENSION.md

# PHP / Composer / tests
/vendor/
composer.lock
wp-tests-config.php
.phpunit.result.cache

# Playwright / MCP scratch
.playwright-mcp/

# Local screenshots / scratch images
*.png
